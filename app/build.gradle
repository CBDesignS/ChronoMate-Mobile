plugins {
    id "com.android.application"
}

android {
    namespace "uk.co.cbdesigns.chronomatefiretab"
    compileSdk 34

    defaultConfig {
        applicationId "uk.co.cbdesigns.chronomatefiretab"
        minSdk 22
        targetSdk 34
        versionCode 3
        versionName "0.2.1"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile("proguard-android-optimize.txt"),
                    "proguard-rules.pro"
        }
    }
}

dependencies {
}
